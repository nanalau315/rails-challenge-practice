<h1><%= @company.name %></h1>
<h3>Company's office:</h3>
<% if @company.offices.length < 1%>
<p>No offices!!</p>
<% else %>
    <ul>
    <% @company.offices.each do |o| %>
    <li><%= o.building.name %> || Floor: <%= o.floor %> 👽 </li>
    <% end %>
</ul>
<% end %>
<h3>All Employees:</h3>
<% @company.employees.each do |e| %>
<li><%= link_to e.name, employee_path(e) %></li><br>
<% end%>
<%= button_to "Add Employee", add_employee_path, method: :get %><br>
<%# button_to "Delete Employee", delete_employee_path, method: :delete %>
<h4>Total Rent this Company is Paying:</h4>
$<%= @company.total_rent %>!!!